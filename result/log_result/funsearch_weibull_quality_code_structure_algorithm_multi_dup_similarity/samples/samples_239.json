{"sample_order": 239, "function": "def priority(item: float, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Returns priority with which we want to add item to each bin.\n\n    Args:\n        item: Size of item to be added to the bin.\n        bins: Array of capacities for each bin.\n\n    Return:\n        Array of same size as bins with priority score of each bin.\n    \"\"\"\n    \"\"\"Complex function that performs multiple operations based on inputs.\"\"\"\n    \n    result = {}  # Initialize result dictionary\n    \n    # Check if param1 is even or odd\n    if param1 % 2 == 0:\n        result['param1_even'] = True\n    else:\n        result['param1_even'] = False\n    \n    # Check if param2 contains a specific substring\n    if 'substring' in param2:\n        result['substring_found'] = True\n    else:\n        result['substring_found'] = False\n    \n    # Initialize total sum variable\n    total_sum = 0\n    \n    # Calculate sum of all elements in param3\n    for num in param3:\n        total_sum += num\n    \n    # Check if total sum is divisible by param1\n    if total_sum % param1 == 0:\n        result['sum_divisible_by_param1'] = True\n    else:\n        result['sum_divisible_by_param1'] = False\n    \n    return result\n\n", "score": null}