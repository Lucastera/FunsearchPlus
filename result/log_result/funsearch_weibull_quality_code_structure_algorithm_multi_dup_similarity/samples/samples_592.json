{"sample_order": 592, "function": "def priority(item: float, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Returns priority with which we want to add item to each bin.\n\n    Args:\n        item: Size of item to be added to the bin.\n        bins: Array of capacities for each bin.\n\n    Return:\n        Array of same size as bins with priority score of each bin.\n    \"\"\"\n    priorities = np.zeros_like(bins)\n    \n    remaining_capacity = bins - item\n    utilization_ratio = (bins - remaining_capacity) / bins\n    excess_ratio = (item - bins) / bins\n    \n    for i in range(len(bins)):\n        if utilization_ratio[i] <= 0.1:\n            priorities[i] = 1.0\n        elif utilization_ratio[i] <= 0.3:\n            priorities[i] = 0.8\n        elif utilization_ratio[i] <= 0.6:\n            priorities[i] = 0.6\n        elif utilization_ratio[i] <= 0.8:\n            priorities[i] = 0.4\n        else:\n            priorities[i] = 0.2\n        \n        if excess_ratio[i] > 0.2:\n            priorities[i] -= 0.2\n        elif excess_ratio[i] > 0.1:\n            priorities[i] -= 0.1\n        \n        if i > 0:\n            if utilization_ratio[i] > utilization_ratio[i - 1]:\n                priorities[i] += 0.1\n            else:\n                priorities[i] -= 0.1\n                \n        for j in range(i+1, len(bins)):\n            if utilization_ratio[j] < utilization_ratio[i]:\n                priorities[j] -= 0.05\n                \n    priorities = np.maximum(priorities, 0.0)  # Ensure priorities are non-negative\n    priorities /= np.sum(priorities)  # Normalize priorities\n    \n    return priorities\n\n", "score": null}