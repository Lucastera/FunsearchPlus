{"sample_order": 837, "function": "def priority(item: float, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Returns priority with which we want to add item to each bin.\n\n    Args:\n        item: Size of item to be added to the bin.\n        bins: Array of capacities for each bin.\n\n    Return:\n        Array of same size as bins with priority score of each bin.\n    \"\"\"\n    \"\"\"Improved version of `priority_v2` with more complex algorithm.\"\"\"\n    if item <= 0:\n        raise ValueError(\"Item size must be greater than 0.\")\n    \n    if len(bins) == 0:\n        raise ValueError(\"Bins array must not be empty.\")\n    \n    priorities = np.zeros_like(bins)\n    \n    for i in range(len(bins)):\n        if bins[i] <= 0:\n            priorities[i] = np.inf\n        else:\n            priorities[i] = np.sqrt(item / bins[i])  # Non-linear weighting\n    \n    max_priority = np.max(priorities)\n    min_priority = np.min(priorities)\n    normalized_priorities = (priorities - min_priority) / (max_priority - min_priority)\n    \n    weighted_priorities = np.exp(normalized_priorities)  # Non-linear weighting\n    \n    # Adjust priorities based on item size and bin capacities\n    for idx, val in enumerate(bins):\n        if val < item:\n            weighted_priorities[idx] *= 1.2\n        elif val >= item and val < (2 * item):\n            weighted_priorities[idx] *= 1.1\n        else:\n            weighted_priorities[idx] *= 0.9\n    \n    # Further adjust priorities based on relative capacities of bins\n    total_capacity = np.sum(bins)\n    bin_ratios = bins / total_capacity\n    for idx, ratio in enumerate(bin_ratios):\n        weighted_priorities[idx] *= (1 + ratio)\n    \n    return weighted_priorities\n\n", "score": -2069.2}