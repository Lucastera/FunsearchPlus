{"sample_order": 266, "function": "def priority(item: float, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Returns priority with which we want to add item to each bin.\n\n    Args:\n        item: Size of item to be added to the bin.\n        bins: Array of capacities for each bin.\n\n    Return:\n        Array of same size as bins with priority score of each bin.\n    \"\"\"\n    \"\"\"Complex function that processes input data and returns the result.\"\"\"\n    result = []\n    \n    for i in range(len(data)):\n        if data[i] % 2 == 0:  # Check if number is even\n            processed_value = data[i] * 2  # Double the value for even numbers\n        else:\n            processed_value = data[i] + 1  # Increment by 1 for odd numbers\n        \n        if processed_value > 10:\n            result.append(processed_value / 2)  # Divide by 2 if value is greater than 10\n        else:\n            result.append(processed_value)\n    \n    return np.array(result)\n\n", "score": null}