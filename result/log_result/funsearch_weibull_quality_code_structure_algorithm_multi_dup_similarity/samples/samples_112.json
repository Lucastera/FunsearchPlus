{"sample_order": 112, "function": "def priority(item: float, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Returns priority with which we want to add item to each bin.\n\n    Args:\n        item: Size of item to be added to the bin.\n        bins: Array of capacities for each bin.\n\n    Return:\n        Array of same size as bins with priority score of each bin.\n    \"\"\"\n    \"\"\"Perform complex operations on input data.\"\"\"\n    result = []\n    \n    for i in range(len(data)):\n        if data[i] % 2 == 0:\n            if data[i] < 0:\n                result.append(data[i] ** 2)  # Square negative even numbers\n            else:\n                result.append(data[i] * 3)  # Triple positive even numbers\n        else:\n            if data[i] < 0:\n                result.append(data[i] + 10)  # Add 10 to negative odd numbers\n            else:\n                result.append(data[i] - 5)  # Subtract 5 from positive odd numbers\n    \n    return np.array(result)\n\n", "score": null}