{"sample_order": 459, "function": "def priority(item: float, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Returns priority with which we want to add item to each bin.\n\n    Args:\n        item: Size of item to be added to the bin.\n        bins: Array of capacities for each bin.\n\n    Return:\n        Array of same size as bins with priority score of each bin.\n    \"\"\"\n    \"\"\"Perform a complex computation on the input data.\"\"\"\n    n = len(data)\n    result = np.zeros(n)\n    \n    for i in range(n):\n        if data[i] % 2 == 0:\n            result[i] = data[i] ** 2\n        else:\n            result[i] = (data[i] + 1) * 3\n    \n    for i in range(1, n):\n        result[i] += result[i-1]\n    \n    for i in range(n):\n        if i % 2 == 0:\n            result[i] *= 1.5\n        else:\n            result[i] *= 1.2\n    \n    return result\n\n", "score": null}