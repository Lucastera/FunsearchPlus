{"sample_order": 289, "function": "def priority(item: float, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Returns priority with which we want to add item to each bin.\n\n    Args:\n        item: Size of item to be added to the bin.\n        bins: Array of capacities for each bin.\n\n    Return:\n        Array of same size as bins with priority score of each bin.\n    \"\"\"\n    if arg1 < 0 or arg1 > 100:\n        raise ValueError(\"arg1 must be between 0 and 100.\")\n    \n    result = {}\n    \n    for item in arg3:\n        if len(arg2) % 2 == 0:\n            key = f\"{item}_{len(arg2)}\"\n        else:\n            key = f\"{len(arg2)}_{item}\"\n        \n        if item % 2 == 0:\n            value = item * arg1\n        else:\n            value = item / arg1\n        \n        if key in result:\n            result[key] += value\n        else:\n            result[key] = value\n    \n    return result\n\n", "score": null}