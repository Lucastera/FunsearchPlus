{"sample_order": 869, "function": "def priority(item: float, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Returns priority with which we want to add item to each bin.\n\n    Args:\n        item: Size of item to be added to the bin.\n        bins: Array of capacities for each bin.\n\n    Return:\n        Array of same size as bins with priority score of each bin.\n    \"\"\"\n    \"\"\"Complex function that performs a series of actions based on input parameters.\"\"\"\n    \n    result = \"\"\n    \n    # Perform some action based on param1\n    if param1 < 0:\n        result += \"Negative param1. \"\n    elif param1 == 0:\n        result += \"Zero param1. \"\n    else:\n        result += \"Positive param1. \"\n    \n    # Perform some action based on param2\n    if param2.isalpha():\n        result += \"Alphabetic param2. \"\n    elif param2.isdigit():\n        result += \"Numeric param2. \"\n    else:\n        result += \"Alphanumeric param2. \"\n    \n    # Perform some action based on param3\n    for elem in param3:\n        if isinstance(elem, str):\n            result += \"String element in param3. \"\n        elif isinstance(elem, int):\n            result += \"Integer element in param3. \"\n        else:\n            result += \"Unknown element type in param3. \"\n    \n    return result\n\n", "score": null}