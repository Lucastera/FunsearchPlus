{"sample_order": 78, "function": "def priority(item: float, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Returns priority with which we want to add item to each bin.\n\n    Args:\n        item: Size of item to be added to the bin.\n        bins: Array of capacities for each bin.\n\n    Return:\n        Array of same size as bins with priority score of each bin.\n    \"\"\"\n    \"\"\"Complex function that processes input data in a specific way.\"\"\"\n    output_data = []\n    \n    for data_point in input_data:\n        if data_point % 2 == 0:\n            result = data_point * 2  # Double the data point if it is even\n        else:\n            result = data_point // 2  # Half the data point if it is odd\n        \n        if result < 0:\n            result *= -1  # Take absolute value if result is negative\n        \n        output_data.append(result)\n    \n    return np.array(output_data)\n\n", "score": null}