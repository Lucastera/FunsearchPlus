{"sample_order": 389, "function": "def priority(item: float, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Returns priority with which we want to add item to each bin.\n\n    Args:\n        item: Size of item to be added to the bin.\n        bins: Array of capacities for each bin.\n\n    Return:\n        Array of same size as bins with priority score of each bin.\n    \"\"\"\n    n_bins = len(bins)\n    priorities = np.zeros(n_bins)\n\n    max_capacity = max(bins)\n    min_capacity = min(bins)\n    avg_capacity = np.mean(bins)\n\n    total_capacity = sum(bins)\n    avg_bin_capacity = total_capacity / n_bins\n\n    for i in range(n_bins):\n        remaining_capacity = bins[i] - item\n\n        if remaining_capacity >= 0:\n            priority_score = 1 / (1 + remaining_capacity)\n\n            if bins[i] == max_capacity:\n                priorities[i] = priority_score * 1.8\n            elif bins[i] == min_capacity:\n                priorities[i] = priority_score * 1\n\n            # New feature: prioritize bins that are close to the average capacity\n            elif abs(bins[i] - avg_bin_capacity) <= 0.2 * avg_bin_capacity:\n                priorities[i] = priority_score * 1.5\n            else:\n                priorities[i] = priority_score\n\n            # New feature: prioritize bins that have no items yet\n            if sum(bins) == total_capacity: \n                priorities[i] *= 2\n\n        else:\n            priorities[i] = 0\n\n    return priorities\n\n", "score": null}