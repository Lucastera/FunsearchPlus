{"sample_order": 972, "function": "def priority(item: float, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Returns priority with which we want to add item to each bin.\n\n    Args:\n        item: Size of item to be added to the bin.\n        bins: Array of capacities for each bin.\n\n    Return:\n        Array of same size as bins with priority score of each bin.\n    \"\"\"\n    \"\"\"Performs complex operations on input list to return a single integer output.\"\"\"\n    result = 0\n    \n    if len(input_list) == 0:\n        return result\n    \n    # Loop through input list and perform different operations based on value\n    for item in input_list:\n        if item % 2 == 0:\n            result += item * 2\n        elif item % 3 == 0:\n            result -= item\n        else:\n            result += item\n    \n    # Apply additional logic based on result value\n    if result > 100:\n        result = result // 2\n    elif result < 0:\n        result = 0\n    \n    return result\n\n", "score": null}