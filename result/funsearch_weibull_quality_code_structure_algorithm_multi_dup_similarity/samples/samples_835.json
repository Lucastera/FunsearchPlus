{"sample_order": 835, "function": "def priority(item: float, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Returns priority with which we want to add item to each bin.\n\n    Args:\n        item: Size of item to be added to the bin.\n        bins: Array of capacities for each bin.\n\n    Return:\n        Array of same size as bins with priority score of each bin.\n    \"\"\"\n    if item <= 0:\n        raise ValueError(\"Item size must be greater than 0.\")\n    \n    if len(bins) == 0:\n        raise ValueError(\"Bins array must not be empty.\")\n    \n    priorities = np.zeros_like(bins)\n    \n    # Calculate priorities based on bin capacities and item size with a more complex algorithm\n    for i in range(len(bins)):\n        if bins[i] <= 0:\n            priorities[i] = np.inf\n        else:\n            if item < 0.5 * bins[i]:\n                priorities[i] = np.sqrt(item / bins[i]) * 1.5\n            elif item >= 0.5 * bins[i] and item < 0.8 * bins[i]:\n                priorities[i] = np.log(item / bins[i]) * 2.0\n            else:\n                priorities[i] = np.cbrt(item / bins[i]) * 1.8\n    \n    # Normalize priorities to be between 0 and 1\n    max_priority = np.max(priorities)\n    min_priority = np.min(priorities)\n    normalized_priorities = (priorities - min_priority) / (max_priority - min_priority)\n    \n    # Apply exponential weighting to priorities for fine-grained adjustments\n    weighted_priorities = np.exp(normalized_priorities) * 1.5\n    \n    # Adjust priorities based on item size and bin capacities with more conditions\n    for idx, val in enumerate(bins):\n        if val < item:\n            weighted_priorities[idx] *= 1.2\n        elif val >= item and val < (2.5 * item):\n            weighted_priorities[idx] *= 1.1\n        else:\n            weighted_priorities[idx] *= 0.9\n    \n    return weighted_priorities\n\n", "score": -2076.0}