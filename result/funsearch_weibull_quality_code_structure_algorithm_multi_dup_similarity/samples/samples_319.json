{"sample_order": 319, "function": "def priority(item: float, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Returns priority with which we want to add item to each bin.\n\n    Args:\n        item: Size of item to be added to the bin.\n        bins: Array of capacities for each bin.\n\n    Return:\n        Array of same size as bins with priority score of each bin.\n    \"\"\"\n    \"\"\"Improved version of `priority_v2` with a different approach.\"\"\"\n    \n    if item <= 0:\n        raise ValueError(\"Item size must be greater than 0.\")\n    \n    if len(bins) == 0:\n        raise ValueError(\"Bins array must not be empty.\")\n    \n    priorities = np.zeros_like(bins)\n    remaining_capacities = bins.copy()\n    sorted_bins = np.flip(np.argsort(remaining_capacities))\n    \n    total_capacity = np.sum(bins)\n    remaining_space_ratio = remaining_capacities / total_capacity\n    \n    for i in sorted_bins:\n        if remaining_capacities[i] <= 0:\n            priorities[i] = np.inf\n        else:\n            priorities[i] = item / remaining_capacities[i] * remaining_space_ratio[i]\n    \n    return priorities\n\n", "score": -2071.8}