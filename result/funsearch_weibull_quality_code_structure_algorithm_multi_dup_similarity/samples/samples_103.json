{"sample_order": 103, "function": "def priority(item: float, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Returns priority with which we want to add item to each bin.\n\n    Args:\n        item: Size of item to be added to the bin.\n        bins: Array of capacities for each bin.\n\n    Return:\n        Array of same size as bins with priority score of each bin.\n    \"\"\"\n    \"\"\"Improved version of `priority_v2`.\"\"\"\n    n_bins = len(bins)\n    priorities = np.zeros(n_bins)\n\n    for i in range(n_bins):\n        remaining_capacity = bins[i] - item\n        if remaining_capacity >= 0:\n            priorities[i] = 1 / (1 + remaining_capacity)\n        else:\n            priorities[i] = 0\n\n    # Adjust priorities based on ratio of remaining capacity to item size\n    max_capacity = max(bins)\n    if max_capacity > 0:\n        priorities *= (bins + item) / max_capacity\n\n    # Normalize priorities\n    total_priority = sum(priorities)\n    if total_priority > 0:\n        priorities /= total_priority\n\n    return priorities\n\n", "score": -2067.0}