{"sample_order": 327, "function": "def priority(item: float, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Returns priority with which we want to add item to each bin.\n\n    Args:\n        item: Size of item to be added to the bin.\n        bins: Array of capacities for each bin.\n\n    Return:\n        Array of same size as bins with priority score of each bin.\n    \"\"\"\n    if item < 0:\n        raise ValueError(\"Item size cannot be negative\")\n\n    max_bin_capacity = np.max(bins)\n    remaining_capacities = max_bin_capacity - bins\n    priorities = np.zeros_like(bins)\n\n    for i, bin_capacity in enumerate(bins):\n        if bin_capacity <= 0:\n            priorities[i] = np.nan\n        else:\n            if item <= bin_capacity:\n                priorities[i] = item / bin_capacity * remaining_capacities[i]\n            else:\n                overflow = item - bin_capacity\n                weighted_overflow = overflow * (max_bin_capacity / (bin_capacity + 1))\n\n                # Adjust priority based on overflow amount\n                if overflow > 0.5 * item:\n                    weighted_overflow *= 2\n                elif overflow < 0.2 * item:\n                    weighted_overflow *= 0.5\n\n                # Check if priority exceeds threshold\n                if weighted_overflow > 0.8 * max_bin_capacity:\n                    priorities[i] = np.inf  # If priority too high, mark as not feasible\n                else:\n                    priorities[i] = weighted_overflow\n\n    return priorities\n\n", "score": -2067.0}