{"sample_order": 747, "function": "def priority(item: float, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Returns priority with which we want to add item to each bin.\n\n    Args:\n        item: Size of item to be added to the bin.\n        bins: Array of capacities for each bin.\n\n    Return:\n        Array of same size as bins with priority score of each bin.\n    \"\"\"\n    \"\"\"Perform complex calculations on input data and return result.\"\"\"\n\n    # Initialize result array\n    result = np.zeros_like(input_data)\n    \n    # Process each element in input data\n    for i in range(len(input_data)):\n        if input_data[i] % 2 == 0:\n            result[i] = input_data[i] * 2\n        elif input_data[i] % 3 == 0:\n            result[i] = input_data[i] * 3\n        else:\n            result[i] = input_data[i] + 1\n            \n    # Filter out negative values\n    result = np.where(result >= 0, result, 0)\n    \n    return result\n\n", "score": null}